import * as React from "react";

const style = {
    color: "#fff",
    fontFamily: "Verdana, Arial",
    fontSize: "70px",
    width: "100%"

}

export const Loading = () => (
    <div style={style} >Loading ...</div>
);
